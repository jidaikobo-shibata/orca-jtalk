# Speech Dispatcher module config (GenericExecuteSynth)
# Place this file under ~/.config/speech-dispatcher/modules/ if you want to load it.

# Module name
AddModule "openjtalk" "generic"

# Path to the wrapper script
GenericExecuteSynth "openjtalk" "/home/shibata/Internal/dev/orca-jtalk/scripts/openjtalk_say.sh"

# Pass the speech data via stdin
GenericExecuteSynthArgs "openjtalk" "$DATA"

# Optional: default language and parameters
# Language "ja"
